# invest

## 2026-08-13 下載方式修改紀錄

- **統一投信 00981A、00403A、00988A 改用官方 Excel 直連下載**
  - 直接呼叫統一投信 `AssetExcelNPOI` 官方端點，不再開啟商品頁並等待 `networkidle`。
  - 下載後檢查 HTTP 狀態、檔案大小與 XLSX ZIP 標頭，再以暫存檔安全覆寫當日檔案。
- **群益投信 00992A、00982A 改用穩定的頁面下載流程**
  - 商品頁改為等待 `domcontentloaded`，避免背景連線使 `networkidle` 持續逾時。
  - 下載前確認頁面標題中的 ETF 代碼，下載後驗證 XLSX 內容，避免基金映射錯誤或儲存到無效檔案。
- 已實測五檔官方檔案均可下載與解析；00992A、00982A 的工作表內容與既有官方下載專案一致。
- **補齊 2026-08-12 缺漏資料**
  - `00403A`、`00981A`、`00982A`、`00988A`、`00992A` 使用 FinMind 指定日期持股資料回補，並保留官方來源正常的 `00990A`、`00991A`。
  - FinMind 回補檔明確標記 `Holding_Source=finmind`，同時保存官方化 CSV、原始 API JSON、基金淨資產估算方式及重建後的歷史／比對資料。

詳細更新內容請參閱 `AI_CONTEXT.md`。

## 2026-08-04 主要修改紀錄
- **修正 00981A 解析錯誤導致股數為 0 的問題**
  - 修復 `etf_tracker.py` 讀取 `00981A` 時誤判期貨表頭的問題，確保正確抓取股票股數。
  - 已重產受影響的歷史資料與 HTML 網頁。
詳細更新內容請參閱 `AI_CONTEXT.md`。
